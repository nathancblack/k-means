# K-Means Image Segmentation

## Install dependencies
```
pip install -r requirements.txt
```

## Run
```
python main.py
```

This runs all three k-means implementations (scikit-learn, non-optimized, and optimized) on the provided images and displays the results side by side.
